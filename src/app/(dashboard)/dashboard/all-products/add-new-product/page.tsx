import { Box } from "@mui/material";

export default function AddNewProduct() {
  return (
    <Box
      sx={{
        border: '.5px solid',
        borderColor: 'grey.400',
        borderRadius: '1rem',
        overflow: 'hidden',
        padding: '1.5rem'
      }}
    >

    </Box>
  );
};
